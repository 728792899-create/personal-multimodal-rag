# Graph-lite 多跳关系固定证据

GraphAlpha1 uses GraphBridge1. GraphBridge1 supports GraphTarget1.

GraphAlpha2 depends on GraphBridge2. GraphBridge2 requires GraphTarget2.

GraphAlpha3 contains GraphBridge3. GraphBridge3 uses GraphTarget3.

GraphAlpha4 supports GraphBridge4. GraphBridge4 depends on GraphTarget4.

GraphAlpha5 requires GraphBridge5. GraphBridge5 contains GraphTarget5.

GraphAlpha6 uses GraphBridge6. GraphBridge6 requires GraphTarget6.

GraphAlpha7 depends on GraphBridge7. GraphBridge7 supports GraphTarget7.

GraphAlpha8 contains GraphBridge8. GraphBridge8 depends on GraphTarget8.

GraphAlpha9 supports GraphBridge9. GraphBridge9 uses GraphTarget9.

GraphAlpha10 requires GraphBridge10. GraphBridge10 supports GraphTarget10.
