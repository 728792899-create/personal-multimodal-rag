# 已知边界

- 默认 hash/mock embedding 只用于本地演示，不代表生产向量质量。
- 当前更适合个人知识库原型，不是多租户企业知识库。
- 大规模文档索引、权限隔离、审计合规和严格评测集仍需补强。
- Chroma、pgvector、真实 LLM 和 cross-encoder reranker 是可选增强，不是默认演示依赖。
- URL 导入和 OCR 的稳定性依赖外部网页质量及本机运行时。

